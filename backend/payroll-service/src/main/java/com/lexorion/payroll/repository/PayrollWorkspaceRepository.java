package com.lexorion.payroll.repository;import com.lexorion.payroll.domain.PayrollWorkspace;import java.util.*;import org.springframework.data.jpa.repository.*;import org.springframework.data.repository.query.Param;public interface PayrollWorkspaceRepository extends JpaRepository<PayrollWorkspace,UUID>{@Lock(jakarta.persistence.LockModeType.PESSIMISTIC_WRITE)@Query("select w from PayrollWorkspace w where w.id=:id")Optional<PayrollWorkspace> findByIdForUpdate(@Param("id")UUID id);}
