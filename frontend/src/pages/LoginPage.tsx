import{useState,type FormEvent}from"react";import{Navigate,useNavigate}from"react-router-dom";import{useAuth}from"../auth/AuthProvider";import{ApiError}from"../api/types";import{Button}from"../components/ui/Button";
export function LoginPage(){const auth=useAuth(),navigate=useNavigate(),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);if(auth.user)return <Navigate to="/app" replace/>;async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");try{await auth.login(email,password);navigate("/app",{replace:true});}catch(x){setError(x instanceof ApiError?x.message:"Sign in failed");}finally{setBusy(false)}}return <main className="login"><form onSubmit={e=>void submit(e)}><img src="/brand.svg"/><h1>Welcome to Horizon</h1><p>Sign in with your Platform account.</p>{error&&<div className="notice error">{error}</div>}<label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></label><Button variant="primary" disabled={busy}>{busy?"Signing in…":"Sign in"}</Button></form></main>}
