import{Page,Notice}from"../components/ui/Page";import{useAuth}from"../auth/AuthProvider";import{useWorkspace}from"../workspace/WorkspaceProvider";export function HomePage(){const a=useAuth(),w=useWorkspace();return <Page title="Horizon" description="Select an organization and product workspace to begin.">{!a.organization?<Notice tone="info">Choose an organization from the header.</Notice>:!w.workspace?<Notice tone="info">Choose an active workspace. Workforce and Payroll APIs require this selector.</Notice>:<div className="cards"><article><h3>{a.organization.organizationName}</h3><p>Role: {a.organization.membershipRole}</p></article><article><h3>{w.workspace.displayName}</h3><p>{w.workspace.productName} · {w.workspace.key}</p></article></div>}</Page>}
